[package]
name = "cuda-resolve"
version = "0.2.0"
edition = "2021"
description = "A2A deliberative engine — uses cuda-equipment shared types"

[dependencies]
# Uncomment when cuda-equipment is published:
# cuda-equipment = { path = "../cuda-equipment" }
