[package]
name = "cuda-resolve"
version = "0.1.0"
edition = "2021"
description = "A2A deliberative compilation — intention to computation via multi-agent deliberation"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
